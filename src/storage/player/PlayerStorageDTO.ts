// DTO (Data Transfer Object) é uma convenção de tipagens, objetos que utilizamos para transferir de um lugar para outro, por exemplo, transferir da aplicação para uma API, da aplicação para o armazenamento.  

export type PlayerStorageDTO = {
	name: string;
	team: string;
};
